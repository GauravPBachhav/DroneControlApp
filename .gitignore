# ❌ PROBLEM: This .gitignore is INCOMPLETE — secrets will get uploaded to GitHub!

# Default ignores
*.iml
.gradle
/local.properties
/.idea
/build
/captures
.externalNativeBuild
.cxx

# Sensitive files — NEVER push these
google-services.json
*.jks
*.keystore
keystore.properties
signing-config.properties